import { ComingSoon } from '@/components/shared/ComingSoon';

export const metadata = { title: 'Laboratory' };

export default function LabPage() {
  return (
    <ComingSoon
      title="Laboratory"
      description="The laboratory hub consolidating order management, result tracking, and specimen workflows is being set up."
    />
  );
}
