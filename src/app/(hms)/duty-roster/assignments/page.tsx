export const metadata = { title: 'Staff Assignment' };

export default function StaffAssignmentPage() {
  return (
    <div className="p-6">
      <h1 className="text-2xl font-semibold tracking-tight">Staff Assignment</h1>
      <p className="text-muted-foreground mt-1 text-sm">
        Assign staff to shifts by date, detect conflicts, and enforce minimum coverage — Phase 6.
      </p>
    </div>
  );
}
