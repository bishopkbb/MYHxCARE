import { ComingSoon } from '@/components/shared/ComingSoon';

export const metadata = { title: 'On-Call Schedule' };

export default function OnCallSchedulePage() {
  return (
    <ComingSoon
      title="On-Call Schedule"
      description="On-call rota management, emergency cover scheduling, and after-hours duty assignment are coming soon."
    />
  );
}
