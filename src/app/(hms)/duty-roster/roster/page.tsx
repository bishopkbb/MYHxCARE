import { ComingSoon } from '@/components/shared/ComingSoon';

export const metadata = { title: 'Duty Roster Calendar' };

export default function DutyRosterCalendarPage() {
  return (
    <ComingSoon
      title="Duty Roster Calendar"
      description="A full calendar view of department rosters, shift patterns, and staff availability is being built."
    />
  );
}
