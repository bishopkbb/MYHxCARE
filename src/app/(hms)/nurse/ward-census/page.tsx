import { WardCensusWorkspace } from '@/features/nursing/components/WardCensusWorkspace';

export const metadata = { title: 'Ward Census' };

export default function NurseWardCensusPage() {
  return <WardCensusWorkspace />;
}
