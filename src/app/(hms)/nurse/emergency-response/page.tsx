import { ComingSoon } from '@/components/shared/ComingSoon';

export const metadata = { title: 'Emergency Response' };

export default function NurseEmergencyResponsePage() {
  return (
    <ComingSoon
      title="Emergency Response"
      description="One-tap ward emergency alerting — code team paging, crash cart checklist, and incident log — is on the way."
    />
  );
}
