import { ComingSoon } from '@/components/shared/ComingSoon';

export const metadata = { title: 'Radiology Requests' };

export default function NurseRadiologyRequestsPage() {
  return (
    <ComingSoon
      title="Radiology Requests"
      description="Requesting and tracking imaging orders on behalf of the ward team is on the way."
    />
  );
}
