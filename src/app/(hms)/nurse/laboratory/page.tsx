import { LaboratoryWorkspace } from '@/features/nursing/components/LaboratoryWorkspace';

export const metadata = { title: 'Laboratory' };

export default function NurseLaboratoryPage() {
  return <LaboratoryWorkspace />;
}
