import { ComingSoon } from '@/components/shared/ComingSoon';

export const metadata = { title: 'Nursing Notes' };

export default function NurseNursingNotesPage() {
  return (
    <ComingSoon
      title="Nursing Notes"
      description="Free-text and structured shift notes per patient, with author, timestamp, and doctor visibility, are on the way."
    />
  );
}
