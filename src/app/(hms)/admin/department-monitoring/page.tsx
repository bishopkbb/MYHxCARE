import { ComingSoon } from '@/components/shared/ComingSoon';

export const metadata = { title: 'Department Monitoring' };

export default function AdminDepartmentMonitoringPage() {
  return (
    <ComingSoon
      title="Department Monitoring"
      description="A real-time overview of all departments and their key operational metrics is being finalised."
    />
  );
}
