import { ComingSoon } from '@/components/shared/ComingSoon';

export const metadata = { title: 'Label Printing' };

export default function LabelPrintingPage() {
  return (
    <ComingSoon
      title="Label Printing"
      description="Printing patient-ready medication labels straight from a dispensed prescription is on the way."
    />
  );
}
