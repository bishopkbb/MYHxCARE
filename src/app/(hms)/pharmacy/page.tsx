import { ComingSoon } from '@/components/shared/ComingSoon';

export const metadata = { title: 'Pharmacy' };

export default function PharmacyPage() {
  return (
    <ComingSoon
      title="Pharmacy"
      description="Prescription dispensing, drug inventory management, pharmacist review workflows, and formulary controls are on the way."
    />
  );
}
