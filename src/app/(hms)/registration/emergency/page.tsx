import { ComingSoon } from '@/components/shared/ComingSoon';

export const metadata = { title: 'Emergency Registration' };

export default function EmergencyRegistrationPage() {
  return (
    <ComingSoon
      title="Emergency Registration"
      description="Fast-path registration for emergency arrivals — minimal fields, immediate MRN issuance — is being built."
    />
  );
}
