import { ComingSoon } from '@/components/shared/ComingSoon';

export const metadata = { title: 'Patient Profile' };

export default function RegistrationPatientProfilePage() {
  return (
    <ComingSoon
      title="Patient Profile"
      description="A registration-focused patient profile — demographics, insurance, and administrative history — is being built."
    />
  );
}
