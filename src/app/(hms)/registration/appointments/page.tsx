import { ComingSoon } from '@/components/shared/ComingSoon';

export const metadata = { title: 'Appointment Scheduling' };

export default function RegistrationAppointmentSchedulingPage() {
  return (
    <ComingSoon
      title="Appointment Scheduling"
      description="Booking, rescheduling, and managing patient appointments from the front desk is being built."
    />
  );
}
