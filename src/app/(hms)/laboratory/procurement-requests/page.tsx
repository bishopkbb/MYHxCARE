import { ComingSoon } from '@/components/shared/ComingSoon';

export const metadata = { title: 'Procurement Requests' };

export default function LaboratoryProcurementRequestsPage() {
  return (
    <ComingSoon
      title="Procurement Requests"
      description="Raising and approving requests for reagents, consumables, and equipment is on the way."
    />
  );
}
