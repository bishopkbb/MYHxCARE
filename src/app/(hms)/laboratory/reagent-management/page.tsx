import { ComingSoon } from '@/components/shared/ComingSoon';

export const metadata = { title: 'Reagent Management' };

export default function ReagentManagementPage() {
  return (
    <ComingSoon
      title="Reagent Management"
      description="Reagent lot tracking, expiry, and open-vial stability windows are on the way."
    />
  );
}
