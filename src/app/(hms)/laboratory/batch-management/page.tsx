import { ComingSoon } from '@/components/shared/ComingSoon';

export const metadata = { title: 'Batch Management' };

export default function LaboratoryBatchManagementPage() {
  return (
    <ComingSoon
      title="Batch Management"
      description="Lot-level tracking and quarantine holds for reagent and consumable batches are on the way."
    />
  );
}
