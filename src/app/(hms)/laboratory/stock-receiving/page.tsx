import { ComingSoon } from '@/components/shared/ComingSoon';

export const metadata = { title: 'Stock Receiving' };

export default function LaboratoryStockReceivingPage() {
  return (
    <ComingSoon
      title="Stock Receiving"
      description="Confirming goods receipts against purchase orders and updating live inventory is on the way."
    />
  );
}
