import { ComingSoon } from '@/components/shared/ComingSoon';

export const metadata = { title: 'Expiry Management' };

export default function LaboratoryExpiryManagementPage() {
  return (
    <ComingSoon
      title="Expiry Management"
      description="A bucketed view of reagents and supplies approaching expiry is on the way."
    />
  );
}
