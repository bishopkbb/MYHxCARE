import { ComingSoon } from '@/components/shared/ComingSoon';

export const metadata = { title: 'Instrument Maintenance' };

export default function InstrumentMaintenancePage() {
  return (
    <ComingSoon
      title="Instrument Maintenance"
      description="Preventive and corrective maintenance logs for every lab instrument are on the way."
    />
  );
}
