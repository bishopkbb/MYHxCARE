import { ComingSoon } from '@/components/shared/ComingSoon';

export const metadata = { title: 'Equipment Calibration' };

export default function EquipmentCalibrationPage() {
  return (
    <ComingSoon
      title="Equipment Calibration"
      description="Calibration schedules, due dates, and certificates for every analyzer are on the way."
    />
  );
}
