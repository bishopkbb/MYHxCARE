import { ComingSoon } from '@/components/shared/ComingSoon';

export const metadata = { title: 'Equipment Reports' };

export default function EquipmentReportsPage() {
  return (
    <ComingSoon
      title="Equipment Reports"
      description="Uptime, calibration compliance, and maintenance history across every instrument are on the way."
    />
  );
}
