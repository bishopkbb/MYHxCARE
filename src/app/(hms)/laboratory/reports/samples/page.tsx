import { ComingSoon } from '@/components/shared/ComingSoon';

export const metadata = { title: 'Sample Reports' };

export default function SampleReportsPage() {
  return (
    <ComingSoon
      title="Sample Reports"
      description="Collection, rejection, and processing-time reporting across every sample is on the way."
    />
  );
}
