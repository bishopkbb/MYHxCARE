import { ComingSoon } from '@/components/shared/ComingSoon';

export const metadata = { title: 'Notifications' };

export default function NotificationsPage() {
  return (
    <ComingSoon
      title="Notifications"
      description="Unified alerts, clinical escalations, system notifications, and preference management are being developed."
    />
  );
}
