import { ComingSoon } from '@/components/shared/ComingSoon';

export const metadata = { title: 'Settings' };

export default function SettingsPage() {
  return (
    <ComingSoon
      title="Profile & Settings"
      description="User profile management, notification preferences, security settings, display options, and account controls are on the way."
    />
  );
}
