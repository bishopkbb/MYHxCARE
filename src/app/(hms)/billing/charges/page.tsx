import { ComingSoon } from '@/components/shared/ComingSoon';

export const metadata = { title: 'Billing & Charges' };

export default function BillingChargesPage() {
  return (
    <ComingSoon
      title="Billing & Charges"
      description="Service charge capture, tariff configuration, insurance billing, NHIS claims, and invoice generation are on the way."
    />
  );
}
