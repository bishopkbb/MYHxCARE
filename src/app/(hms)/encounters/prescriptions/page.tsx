import { ComingSoon } from '@/components/shared/ComingSoon';

export const metadata = { title: 'Prescriptions' };

export default function PrescriptionsPage() {
  return (
    <ComingSoon
      title="Prescriptions"
      description="Digital prescription writing, drug interaction checks, dosage calculators, and e-prescribing workflows are on the way."
    />
  );
}
