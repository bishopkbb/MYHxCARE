import { ComingSoon } from '@/components/shared/ComingSoon';

export const metadata = { title: 'Orders Management' };

export default function OrdersManagementPage() {
  return (
    <ComingSoon
      title="Orders Management"
      description="A unified view of every lab, imaging, and medication order placed from the emergency department is on the way."
    />
  );
}
