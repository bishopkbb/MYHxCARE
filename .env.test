# Test environment — loaded by Vitest via @next/env in src/test/setup.ts.
# Values here are fake but structurally valid so Zod validation passes.
# Never use real credentials or staging/production URLs here.

NEXT_PUBLIC_API_BASE_URL=http://localhost:8080/api/v1
NEXT_PUBLIC_WS_URL=ws://localhost:8080/ws
NEXT_PUBLIC_APP_ENV=development
